# فایل: simple_encrypt.py

key = input("عبارت کلید رو وارد کن: ")
text = input("متن رو وارد کن: ")

# رمزنگاری خیلی ساده با XOR
encrypted = ''.join(chr(ord(c) ^ ord(key[i % len(key)])) for i, c in enumerate(text))

print("\n✅ متن رمزنگاری شده:")
print(encrypted)
